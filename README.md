# Vertex Web Solutions

Modern single-page agency website built with React, Vite, Tailwind CSS, Framer Motion, and React Icons.

## Run Locally

```bash
npm install
npm run dev
```

Open `http://localhost:5173`.

## Build

```bash
npm run build
```

The production output is generated in `dist/`.
